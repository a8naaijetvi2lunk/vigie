{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main, settings and sessions windows",
  "windows": ["main", "settings", "sessions"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
